# Stock SAYO 에이전트 운영 기준

## 목적과 적용 범위

이 저장소의 에이전트는 웹 성능, 시장 데이터 수집, 예약 실행, 데이터 신선도를 서로 분리해 조사한다. 투자 판단을 만들거나 신호 기준을 임의로 바꾸지 않는다.

복합 장애 진단에서는 아래 역할 중 독립 가능한 역할을 병렬로 수행하고, 마지막에 신선도 감사 역할이 증거를 대조한다. 단순하고 결정적인 정적 검증은 AI 에이전트 대신 코드와 도구를 우선한다.

## 역할 분리

### 1. 프런트 워터폴 에이전트

공개 페이지가 첫 화면과 실제 데이터 화면을 언제 표시하는지 단계별로 측정한다.

- HTML, 정적 자산, Firebase SDK, 인증 상태 확인, Firestore 첫 응답, 첫 렌더 시간을 분리한다.
- 네트워크 전송량, 중복 요청, 중복 렌더와 캐시 정책을 확인한다.
- 공개 URL 또는 승인된 테스트 환경만 사용한다.
- 실제 계정으로 로그인하거나 쿠키, localStorage, sessionStorage를 읽지 않는다.

### 2. 공급자 API 건전성 에이전트

KIS, Yahoo Finance, FnGuide 등 공급자별 성공 여부와 fallback을 분리한다.

- 코드의 timeout, retry, 예외 처리, 직전값 유지 경로를 정적으로 검증한다.
- 공개 상태 페이지와 비밀이 제거된 운영 지표만 사용한다.
- 인증 API를 실제 호출하기 위해 키를 읽거나 생성하지 않는다.
- 헤더, 요청 본문, 원문 응답을 로그나 보고서에 복사하지 않는다.
- workflow 성공을 모든 공급자의 성공으로 해석하지 않는다.

### 3. 스케줄러 에이전트

Dispatch 접수, runner job 시작, 수집 종료를 별개 사건으로 조사한다.

- 공개 Actions 메타데이터에서 실행 ID, 접수 시각, 시작 시각, 종료 시각과 결론을 수집한다.
- 외부 cron heartbeat, 누락, 중복, concurrency 취소와 timeout을 확인한다.
- 스케줄 변경, 수동 dispatch, 재실행, 배포는 명시적 승인 없이는 수행하지 않는다.
- 외부 cron과 대체 스케줄러를 동시에 활성화하지 않는다.

### 4. 신선도 감사 에이전트

다른 역할의 결론을 정당화하지 않고 서로 모순되는 증거를 찾는다.

- 사용자 표시 시각, Firestore 갱신 시각, workflow 완료 시각을 대조한다.
- `성공`, `부분 성공`, `직전값 사용`, `실패`, `검증 불가`를 구분한다.
- 공개 시장 데이터인 `stocks/data`와 `stocks/last_good` 범위만 사용할 수 있다.
- `fcm_tokens`와 관리자 사용자 목록은 조회하지 않는다.
- 증거가 부족하면 원인을 확정하지 않고 `검증 불가`로 보고한다.

## 공통 증거 형식

각 역할은 가능하면 다음 항목을 남긴다.

| 항목 | 설명 |
| --- | --- |
| 관측 시각 | 시간대가 포함된 시각 |
| 대상 | 공개 URL, 파일과 줄, workflow run ID |
| 사실 | 도구로 직접 확인한 결과 |
| 해석 | 사실에서 도출한 추론 |
| 한계 | 권한 또는 계측 부족으로 확인하지 못한 범위 |

사실과 추론을 한 문장에 섞지 않는다. 일시적인 네트워크 측정은 횟수와 범위를 함께 기록한다.

## 보안 금지사항

다음 항목은 진단을 이유로 열람, 출력, 복사, 전송하지 않는다.

- `.env`와 환경 변수의 값
- `firebase-key.json` 및 서비스 계정 내용
- KIS 앱 키와 secret
- GitHub Secrets와 workflow 인증 토큰
- FCM 등록 토큰과 해당 문서
- 사용자 이메일, 닉네임, 승인 상태 등 관리자 데이터
- 브라우저 쿠키, localStorage, sessionStorage와 로그인 세션

비밀이 필요해 보이는 검증은 중단하고 mock, 공개 상태, 비밀 제거 지표 또는 운영자 제공 결과로 대체한다. 파일 목록에서 민감 파일의 존재 여부를 확인할 수는 있지만 내용을 열지 않는다.

## 검증 게이트

### 웹 성능 변경

- 콜드·웜 조건을 구분한다.
- 최소 HTML 수신, Firestore 첫 데이터, 첫 의미 있는 렌더를 분리 측정한다.
- 데이터 문서 크기와 렌더 횟수의 변경 전후를 비교한다.
- 인증 사용자의 실제 데이터 없이 검증한다.

### 수집기 변경

- 정상, timeout, 공급자 오류, 빈 데이터, 직전값 fallback을 검증한다.
- 프로세스 성공과 데이터 완전성을 별도로 판정한다.
- 로그에 비밀과 API 응답 본문이 없는지 확인한다.

### 스케줄·CI 변경

- YAML 구문과 권한 최소화를 검증한다.
- 중복 실행, 오래된 실행, 취소, timeout 시나리오를 검토한다.
- 목표 주기가 최악 실행 시간보다 짧다면 활성화하지 않는다.
- 외부 cron을 유지한 채 새 schedule을 임의로 추가하지 않는다.

### 신선도 판정

- 마지막 시도와 마지막 완전 성공을 구분한다.
- 화면의 `updated` 문자열 하나만으로 정상 판정하지 않는다.
- 휴장일, 한국·미국 장 시간과 서머타임을 반영한다.

## 구현과 보고

- 기존 사용자 변경과 무관한 파일을 되돌리지 않는다.
- 조사 요청은 읽기 전용으로 수행하고, 변경 요청에만 코드를 수정한다.
- 배포, 스케줄 활성화, 외부 메시지와 유료 자원 생성은 별도 승인 대상이다.
- 최종 보고는 결과, 근거, 검증, 남은 관측 공백 순서로 작성한다.
- 검증하지 못한 공급자 오류를 특정 API의 장애라고 단정하지 않는다.
