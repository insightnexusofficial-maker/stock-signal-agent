# Stock SAYO 운영 안내

이 문서는 데이터가 늦게 갱신되거나 웹 화면이 오래 기다릴 때 운영자가 먼저 볼 기준이다. 코드 오류, 스케줄 호출 누락, GitHub 실행 대기, 외부 데이터 공급자 오류를 서로 구분하는 것이 목적이다.

## 현재 데이터 흐름

1. 저장소 밖의 외부 cron 서비스가 GitHub Actions의 `Stock Signal Check`를 호출한다.
2. GitHub-hosted runner가 `uploader.py`를 실행한다.
3. 수집기가 외부 시장 데이터 공급자를 조회하고 Firestore의 공개 시장 데이터 문서를 갱신한다.
4. 웹앱이 Firestore 문서를 직접 구독해 화면을 그린다.

`firebase.json`은 Hosting과 Firestore 규칙만 선언한다. `functions/` 디렉터리가 있더라도 현재 운영 데이터 경로가 Cloud Functions라고 가정하면 안 된다.

## 공식 실적·거시 일정 동기화

Study 사이트의 `data/event-latest.json`이 공식 일정과 검증된 결과의 공개 기준 피드다. 별도 Study 자동화가 매시 05분과 35분 KST에 실행되어 기업 IR·통계기관·중앙은행의 공개 자료만 확인한다. 08:05와 23:05에는 전체 일정의 신규·변경 여부를 확인하고, 나머지 실행은 실제 공식 발표 시각이 지난 due 이벤트가 있을 때만 결과를 수집한다.

`uploader.py`는 기존 시장 데이터 수집 시 이 피드를 한 번 읽어 `stocks/data.event_calendar`에 함께 발행한다. 피드가 만료됐거나 형식·해시·공식 출처 검증에 실패하면 기존 이벤트 값을 유지하고 `event_sync_status`만 실패로 기록한다. 이 실패는 주가·밸류에이션·사이클 데이터의 완전 성공으로 해석하지 않는다.

- 기업 실적 결과는 Study 검증과 별도 고강도 감사가 끝난 경우에만 사이클 근거 후보로 사용할 수 있다.
- CPI, 고용, PCE, FOMC, 한국 CPI, 한국은행 일정과 결과는 설명용이다. 매수·매도 신호, 점수, 정렬, 종목 수집 완전성에는 직접 반영하지 않는다.
- 현지 발표 시각은 원문의 IANA 시간대로 해석해 KST `+09:00`으로 저장한다. 미국 서머타임을 고정 오프셋으로 추정하지 않는다.
- 공식 발표 시각을 확인하지 못한 일정은 날짜만 표시하며 관행이나 전년도 패턴으로 due 처리·결과 수집·알림하지 않는다.
- 결과 수집 실패 시 빈 결과로 덮어쓰지 않고 마지막 검증 피드를 보존한다.

공식 결과가 `complete + verified`로 동기화되면 다음 SAYO 30분 수집에서 결과 확인 알림을 한 번 보낸다. 알림에는 공식 발행 시각을 KST로 표시하며, 공식 원문·기준기간·수치·단위가 빠진 결과는 보내지 않는다.

객관적 기준을 넘은 공식 결과만 `shock`로 분류할 수 있다. 기준과 판정 이유가 피드에 있고, 결과가 완전·검증·감사 완료 상태여야 한다. `Event Shock Alert` 워크플로는 매일 07:00 KST에 알림 대상을 확인하며, 같은 이벤트는 전송 성공 뒤 상태 문서로 중복을 막는다. GitHub 예약 실행은 플랫폼 사정으로 몇 분 늦게 시작될 수 있다.

현재 기준은 기업 매출·공식 가이던스·공식 투자계획의 20% 이상 변화, CPI/PCE 전월비 절대 0.6% 이상 또는 전년비 변화폭 0.5%p 이상, 비농업고용 절대 50만 명 이상 변화, 실업률 0.5%p 이상 변화, FOMC 금리 50bp 이상 변화, 공식 긴급 정책조치다. 시장 반응, 기사 제목, 비공식 컨센서스만으로는 쇼크 알림을 보내지 않는다.

운영 점검 시 다음 세 시각을 분리한다.

1. Study 자동화가 일정을 확인한 시각
2. 공식 결과가 Study 공개 피드에 반영된 시각
3. SAYO `stocks/data`가 해당 피드 ID를 동기화한 시각

`Event Shock Alert`가 성공해도 실제 전송 대상 토큰이 없으면 전송 완료 상태를 쓰지 않는다. FCM 토큰이나 사용자 문서는 점검 목적으로 열람하거나 출력하지 않는다.

## 현재 운영 주기

- 운영 목표와 웹·로컬 안내는 **30분 단위**다.
- 2026년 7월 29일 공개 Actions 기록에서는 국장 장중 09:00~15:30 KST에 30분 단위 실행 14회가 관측됐다.
- 외부 cron의 실제 설정, 시간대, 재시도, 담당자와 상태 페이지는 이 저장소에서 확인할 수 없다.

실행 간격은 공개 Actions 기록으로 감사하되 외부 cron 자체의 heartbeat는 별도 운영 화면에서 확인한다.

## 화면이 갱신되지 않을 때

GitHub Actions의 최신 실행을 열고 아래 순서로 확인한다. 워크플로 요약에는 비밀값 없이 Dispatch 접수 시각, 실제 job 시작 시각, 요약 기록 시각, job 상태가 기록된다.

| 관측 결과 | 우선 의심할 영역 | 다음 확인 |
| --- | --- | --- |
| 예정 시각에 Run 자체가 없음 | 외부 cron 또는 dispatch 인증 | 외부 cron 실행 이력과 응답 코드 |
| Run은 있으나 job 시작이 늦음 | GitHub runner 큐 또는 concurrency | Dispatch와 실제 시작 시각 차이 |
| job이 시작되고 실패함 | 설치, 수집기, Firestore 쓰기 | 실패한 단계명과 비밀이 제거된 오류 분류 |
| job은 성공했지만 화면 데이터가 오래됨 | 부분 성공, 공급자 실패, fallback | 사용자 표시 시각과 마지막 정상 완료 시각 |
| 공개 URL은 빠르지만 화면 전환이 늦음 | 브라우저 SDK, 인증, Firestore 이중 구독, 렌더 | 브라우저 단계별 워터폴 |

`Job 상태: success`는 프로세스가 오류 코드 없이 끝났다는 뜻이다. 각 데이터 공급자와 모든 종목이 최신이라는 보증은 아니다.

## 2026년 7월 20일 기준 장애 예시

- 09:00:28 KST에 접수된 Run은 11:58:19에 job을 시작해 12:04:48에 끝났다.
- 실제 signal checker 실행은 약 6분이었지만 runner 대기는 약 2시간 58분이었다.
- 10:00부터 11:30 사이의 대기 실행은 job을 시작하지 못하고 뒤이은 실행에 의해 취소됐다.
- 12:00 실행은 앞 실행 종료 직후 시작해 정상 완료됐다.

이 사례에서는 “API가 3시간 동안 실행됐다”가 아니라 “실행 환경을 약 3시간 기다렸다”가 정확한 설명이다. 공급자별 API 성공 여부는 별도 계측 없이는 확정할 수 없다.

## 최신 실행 우선 정책

워크플로는 하나의 전역 concurrency 그룹을 사용하고 `cancel-in-progress: true`로 설정한다. 오래된 실행이 runner를 기다리는 동안 새 실행이 들어오면 새 실행을 우선한다.

현재 관측된 외부 호출 간격 30분은 job timeout 25분보다 길어 이 정책과 양립한다. 호출을 10분으로 줄이면 정상 실행도 다음 호출에 취소될 수 있으므로, 먼저 다음 중 하나를 충족해야 한다.

- 정상 실행의 95% 이상이 10분보다 충분히 짧게 끝난다.
- 수집 작업을 분할하거나 중복 실행에 안전하게 만든다.
- GitHub Actions 대신 예약 실행에 적합한 실행 환경으로 옮긴다.

## 외부 cron heartbeat와 경보

스케줄러가 자기 자신을 감시하게 하면 스케줄러 중단 시 경보도 사라진다. 별도 모니터가 GitHub Actions 공개 실행 메타데이터와 Firestore 시장 데이터의 시각만 확인하도록 한다.

현재 30분 주기를 유지한다면 장중 기준 권장 경보다.

- 40분 동안 새 Dispatch 없음: 호출 누락 경고
- Dispatch 후 5분 동안 job 미시작: runner 대기 경고
- Dispatch 후 15분 동안 job 미시작: 심각 경고
- 마지막 정상 데이터가 45분보다 오래됨: 신선도 경고
- 마지막 정상 데이터가 60분보다 오래됨: 심각 경고

목표를 10분으로 확정하면 `새 Dispatch 15분`, `데이터 20분`을 1차 경고 기준으로 다시 정한다. 휴장일과 한국·미국 장 시간, 미국 서머타임을 반드시 반영한다.

외부 cron 관리 화면에는 최소한 다음을 기록한다.

- 서비스 이름과 담당자
- 정확한 주기와 시간대
- 활성 장 시간과 휴장 처리 방식
- 재시도 횟수와 최대 재시도 시간
- 마지막 성공 호출과 마지막 실패 호출
- 호출 실패 알림 수신자

인증 헤더나 토큰 값은 문서, 실행 요약, 알림에 넣지 않는다.

## 권장 전환: Cloud Scheduler와 전용 실행 작업

장기적으로는 Cloud Scheduler가 Cloud Run Job 같은 전용 수집 작업을 실행하도록 옮기는 구성이 권장된다. GitHub-hosted runner 큐를 데이터 갱신 경로에서 제거하고, Secret Manager와 최소 권한 서비스 계정을 사용할 수 있기 때문이다.

전환은 다음 순서로 진행한다.

1. 목표 주기, 장 시간, 시간대, 담당자, 월 비용 한도를 먼저 승인한다.
2. 수집기를 Cloud Run Job으로 패키징하고 비밀값은 Secret Manager에서만 주입한다.
3. Firestore 쓰기 전용 최소 권한 서비스 계정을 사용한다.
4. 비운영 프로젝트 또는 쓰기 없는 검증 방식으로 실행 시간과 공급자별 성공률을 확인한다.
5. 새 Scheduler는 비활성 상태로 구성하고 경보부터 검증한다.
6. 전환 시각에 기존 외부 cron을 먼저 비활성화한 뒤 새 Scheduler 하나만 활성화한다.
7. 첫 실행의 시작, 종료, 데이터 신선도와 중복 쓰기 여부를 확인한다.
8. 문제가 있으면 새 Scheduler를 끄고 기존 cron 하나만 복구한다.

GitHub Actions 화면을 계속 운영 창구로 써야 한다면 Cloud Scheduler가 최소 기능의 인증된 중계 서비스를 호출하고, 중계 서비스가 workflow dispatch를 수행할 수 있다. 이 방식은 runner 큐 의존성이 남으므로 직접 Cloud Run Job을 실행하는 방식보다 우선순위가 낮다.

외부 cron과 Cloud Scheduler를 동시에 활성화하지 않는다. 중복 호출은 공급자 사용량, 비용, Firestore 쓰기와 알림 중복을 늘릴 수 있다.

## 향후 계측 항목

다음 항목은 공개 실행 요약이 아니라 제한된 운영 지표에 기록하되 비밀이나 응답 본문은 저장하지 않는다.

- 공급자 이름, 성공/실패, HTTP 상태 범주, 소요 시간, 재시도 횟수
- 전체/성공/실패/직전값 사용 종목 수
- 마지막 시도 시각, 마지막 부분 성공 시각, 마지막 완전 성공 시각
- Firestore 발행 시작과 완료 시각, 데이터 기준 시각
- 브라우저 HTML, SDK, 인증, Firestore 첫 응답, 첫 렌더 완료 시간

## 보안 원칙

- `.env`, 서비스 계정 파일, KIS 키, GitHub Secrets, Firebase 인증 토큰을 열람하거나 출력하지 않는다.
- FCM 토큰, 닉네임, 이메일 등 사용자 데이터는 상태 점검 대상이 아니다.
- Actions 요약에는 실행 ID, commit, 시각, 단계 결과처럼 공개 가능한 메타데이터만 남긴다.
- 수동 실행, 스케줄 변경, 배포, 외부 서비스 재설정은 운영 책임자의 명시적 승인 후 수행한다.
